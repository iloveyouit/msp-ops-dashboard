# Environment Variables Configuration

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# PostgreSQL connection string
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
# Default for local development (matches docker-compose.yml defaults)
DATABASE_URL="postgresql://mspops:mspops_secret@localhost:5432/msp_ops_dashboard"

# =============================================================================
# SECURITY CONFIGURATION
# =============================================================================
# JWT secret for token signing
# ⚠️  CRITICAL: Change this to a strong, random string in production!
# Generate a secure secret: openssl rand -base64 32
JWT_SECRET="change-me-in-production-use-a-real-secret"

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================
# Application port (default: 3000)
APP_PORT=3000

# Upload directory for evidence files (screenshots, logs, etc.)
UPLOAD_DIR=./uploads

# =============================================================================
# AI PROVIDER CONFIGURATION (Optional)
# =============================================================================
# AI provider for draft assistance
# Options: stub (default, no-op), openai, anthropic
AI_PROVIDER=stub

# API key for AI provider (required if AI_PROVIDER is not 'stub')
AI_API_KEY=

# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================
# 1. Copy this file to .env in the project root:
#    cp .env.example .env
#
# 2. Update the values above, especially:
#    - JWT_SECRET (use a strong random string)
#    - DATABASE_URL (if using a different database configuration)
#
# 3. If using Docker Compose, the DATABASE_URL host should be 'db' instead of 'localhost':
#    DATABASE_URL="postgresql://mspops:mspops_secret@db:5432/msp_ops_dashboard"
